<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <link href="/css/app.css" rel="stylesheet">
  </head>
  <body>
    <script type="text/x-handlebars">
      <nav class="navbar navbar-default" role="navigation">
        <div class="navbar-header">
          {{#linkTo 'index' classNames="navbar-brand"}}App Dojo{{/linkTo}}
        </div>

        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="">{{#linkTo 'users'}}Users{{/linkTo}}</li>
            <li class="">{{#linkTo 'about'}}About{{/linkTo}}</li>
          </ul>
        </div>
      </nav>
      {{ outlet }}
    </script>

    <script type="text/x-handlebars" id="users">
      <div class="container-fluid">
        <div class="row-fluid">
          <div class="span3">
            <table class="table">
              <thead>
                <tr><th>Users</th></tr>
              </thead>
              {{#each model}}
                <tr><td>
                  <a href="#">{{first_name}} {{last_name}} <small class="text-muted">{{email}}</small></a>
                </td></tr>
              {{/each}}
            </table>
          </div>
          <div class="span9">

          </div>
        </div>
      </div>
    </script>

    <script type="text/x-handlebars" id="index">
      <h1>Welcome to App Dojo</h1>
      <p>This is a test for Heroku and Lineman to pull data from the API</p>
    </script>

    <script type="text/x-handlebars" id="about">
      <h1>About Awesomeness</h1>
    </script>

    <script src="/js/app.js" type="text/javascript"></script>
  </body>
</html>

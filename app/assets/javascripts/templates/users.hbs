<div class="container-fluid">
  <div class="row-fluid">
    <div class="span3">
      <table class="table">
        <thead>
        <tr><th>Users</th></tr>
        </thead>
          {{#each model}}
            <tr><td>
              <a href="#">{{first_name}} {{last_name}} <small class="text-muted">{{email}}</small></a>
            </td></tr>
          {{/each}}
      </table>
    </div>
    <div class="span9">

    </div>
  </div>
</div>